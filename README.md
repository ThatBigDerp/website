Open this project either as Github Codespace or locally download the files and install Bun

To install dependencies:
```sh
bun install
```

To run the server:
```sh
bun run dev:server
```

open http://localhost:3000
